#!/bin/bash
cd bin/en
make clean all && mame apple2gs -window -flop1 glider-en.po -resolution 560x384 -printer null_modem -bitb socket.localhost:2001 -modem null_modem -bitb2 socket.localhost:2000 -mouse -debug
